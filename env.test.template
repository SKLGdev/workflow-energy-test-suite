# ============================================
# ENV TEST - Template para Test Suite
# ============================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo a .env en la raíz del proyecto
# 2. Completa los valores con tus credenciales reales
# 3. Ejecuta: cp env.test.template .env
#
# ============================================

# ENTORNO
NODE_ENV=test

# SERVIDOR BACKEND
PORT=5000
FRONTEND_URL=http://localhost:3000
MOBILE_URL=http://localhost:8081

# BASE DE DATOS - SUPABASE
# Obtén estas credenciales en: Supabase Dashboard > Settings > API
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# AUTENTICACIÓN JWT
# Genera un secret seguro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_here
JWT_REFRESH_EXPIRES_IN=7d

# FRONTEND (Vite)
VITE_API_URL=http://localhost:5000

# BASE URL PARA TESTS
BASE_URL=https://workflow-energy.vercel.app

# CREDENCIALES DE USUARIOS PARA TESTS E2E
# Estas credenciales se usan en global-setup.ts para crear estados de autenticación
ADMIN_USER=your_admin_email@example.com
ADMIN_PASS=your_admin_password

SUPERVISOR_USER=your_supervisor_email@example.com
SUPERVISOR_PASS=your_supervisor_password

JEFE_USER=your_jefe_email@example.com
JEFE_PASS=your_jefe_password

EMPLEADO_USER=your_empleado_email@example.com
EMPLEADO_PASS=your_empleado_password

